import RecordsGrid from "../elements/grids/RecordsGrid";

/**
 * Renders the "Records" page
 * @returns {JSX.Element}
 * @constructor
 */
const Records = () => {
    return (
        <div>
            <RecordsGrid />
        </div>
    );
}

export default Records;
